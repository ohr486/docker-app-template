# docker-app-template
template for container apps

## Setup
### Phoenix
```
$ cd phoenix
$ docker-compose build
$ docker-compose up
```
### Rails
```
$ cd rails
$ docker-compose build
$ docker-compose up
```
